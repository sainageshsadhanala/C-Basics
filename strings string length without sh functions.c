#include<stdio.h>
#include<string.h>
void main()
{
	char s[100];
	int len;
	printf("enter a string");
	gets(s);
	for(len=0;s[len]!='\0';len++)
	{
		
	}
	printf("string length=%d",len);
}
